// Placeholder Prisma schema for Ticket 1.3. Models and initial migration
// land in Ticket 1.4.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
